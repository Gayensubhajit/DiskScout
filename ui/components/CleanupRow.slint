export component CleanupRow inherits Rectangle {
    callback review-clicked();
    in property <image> icon;
    in property <string> title: "";
    in property <string> description: "";
    in property <string> size: "";
    in property <string> status-text: "REVIEW FIRST";
    in property <bool> is-safe: false;
    in property <color> icon-tint: #2563eb;
    in property <color> icon-bg: #eff6ff;

    width: 100%;
    height: 76px;
    background: touch.has-hover ? #f8fafc : #ffffff;
    border-radius: 12px;
    border-width: 1px;
    border-color: touch.has-hover ? #cbd5e1 : #e2e8f0;

    HorizontalLayout {
        padding-left: 16px;
        padding-right: 16px;
        spacing: 12px;

        // LEFT: Icon Box (40x40)
        Rectangle {
            width: 40px;
            height: 40px;
            border-radius: 10px;
            background: root.icon-bg;
            y: (parent.height - self.height) / 2;

            Image {
                source: root.icon;
                width: 20px;
                height: 20px;
                x: (parent.width - self.width) / 2;
                y: (parent.height - self.height) / 2;
                colorize: root.icon-tint;
            }
        }

        // CENTER: Title and description (Stretches full remaining width, elides when tight)
        VerticalLayout {
            horizontal-stretch: 1;
            min-width: 80px;
            spacing: 2px;
            alignment: center;

            Text {
                text: root.title;
                color: #0f172a;
                font-size: 14px;
                font-weight: 600;
                overflow: elide;
            }

            Text {
                text: root.description;
                color: #64748b;
                font-size: 12px;
                overflow: elide;
            }
        }

        // RIGHT REGION: Strict aligned columns across all rows

        // 1. Size Column (Fixed 75px width, right-aligned)
        Rectangle {
            width: 75px;
            height: 100%;

            Text {
                text: root.size;
                color: #0f172a;
                font-size: 14px;
                font-weight: 700;
                font-family: "JetBrains Mono";
                horizontal-alignment: right;
                vertical-alignment: center;
                width: 100%;
                y: (parent.height - self.height) / 2;
            }
        }

        // 2. Safety Status Badge Column (Fixed 110px width)
        Rectangle {
            width: 110px;
            height: 100%;

            Rectangle {
                width: 104px;
                height: 24px;
                background: root.is-safe ? #dcfce7 : #fef3c7;
                border-radius: 6px;
                x: (parent.width - self.width) / 2;
                y: (parent.height - self.height) / 2;

                Text {
                    text: root.status-text;
                    color: root.is-safe ? #15803d : #b45309;
                    font-size: 10px;
                    font-weight: 700;
                    x: (parent.width - self.width) / 2;
                    y: (parent.height - self.height) / 2;
                }
            }
        }

        // 3. Review Action Button Column (Fixed 88px width)
        Rectangle {
            width: 88px;
            height: 100%;

            TouchArea {
                width: 88px;
                height: 32px;
                x: 0;
                y: (parent.height - self.height) / 2;
                clicked => { root.review-clicked(); }

                Rectangle {
                    background: parent.has-hover ? #e2e8f0 : #f1f5f9;
                    border-radius: 6px;
                    border-width: 1px;
                    border-color: parent.has-hover ? #94a3b8 : #cbd5e1;

                    Text {
                        text: "Review →";
                        color: #0f172a;
                        font-size: 12px;
                        font-weight: 600;
                        x: (parent.width - self.width) / 2;
                        y: (parent.height - self.height) / 2;
                    }
                }
            }
        }
    }

    touch := TouchArea {
        width: 100%;
        height: 100%;
    }
}
